<?php
require_once 'classes/Gestionnaire.php';

// Vérifier si le gestionnaire est connecté
if (!Gestionnaire::estConnecte()) {
    header('Location: pages/login_gestionnaire.php');
    exit;
}

// Récupérer les informations du gestionnaire
session_start();
$gestionnaire = $_SESSION['gestionnaire'];
?>

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Espace Gestionnaire</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="#">ANACAONA - Espace Gestionnaire</a>
            <div class="navbar-nav ms-auto">
                <span class="navbar-text me-3">
                    Connecté en tant que : <?= htmlspecialchars($gestionnaire['prenom'] . ' ' . $gestionnaire['nom']) ?>
                </span>
                <a href="deconnexion_gestionnaire.php" class="btn btn-outline-light">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h2>Bienvenue dans votre espace gestionnaire</h2>
        <!-- Ajoutez ici le contenu de l'espace gestionnaire -->
    </div>
</body>
</html>